// @ts-nocheck
function labelRelatorioBlock(key) {
  if (key === 'cabecalho') return 'Cabecalho';
  if (key === 'info_geracao') return 'Info de geracao';
  if (key === 'tabela') return 'Tabela';
  if (key === 'rodape') return 'Rodape';
  return key;
}
