// @ts-nocheck
function renderAll() {
  renderTipos();
  renderTipoOptions();
  renderSecoes();
  renderSecaoOptions();
  renderAtributos();
  toggleAtributoTemplateConfig();
  renderLayoutEditor();
  renderDocumentoCampos();
  renderDocumentos();
  refreshMaterializeUI();
}

